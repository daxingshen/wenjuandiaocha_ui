# dev 下 studio:5173 与 runtime:5174 异端口,分享链接需显式指向 runtime。
# 未配置时 resolveRuntimeBase() 回落 location.origin(=5173,指向 studio 自己,错)。
# prod 同源部署无需此项,回落 location.origin 即正确。
VITE_RUNTIME_BASE=http://localhost:5174
