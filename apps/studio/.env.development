# 分享链接的 runtime base(VITE_RUNTIME_BASE)现由 vite.config.ts 在起服时自动探测局域网 IP 注入
# (http://<LAN-IP>:5174),故本机用 localhost 打开 studio,复制的作答链接也是局域网 IP——
# 手机/其它设备可扫码作答。见 apps/studio/vite.config.ts lanHost() 与 src/features/publish/publishLink.ts。
#
# 仅当需要钉死一个固定 runtime 地址(如异源部署、多网卡取错 IP)时,才取消下面这行注释并填值,
# 它优先级最高、盖过自动探测:
# VITE_RUNTIME_BASE=http://<runtime-host>:5174
